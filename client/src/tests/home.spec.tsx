import {describe,expect,test} from "@jest/globals"
import {render, screen} from "@testing-library/react"
import Home from "@/app/page"

describe("comprar produto", () => {
   test("botão de comprar",() => {
      
   }),
   test("verificar se o cliente está logado",() => {
      
   }),
   test("verificar se o endereço do cliente está cadastrado",() => {
      expect(2).toBe(3)
   }),
   test("cadastrando endereço se não houver",() => {
      
   }),
   test("preenchendo cartão",() => {
      
   })
})
describe("alugar produto", () => {
   test("botão de comprar",() => {
      
   }),
   test("verificar se o cliente está logado",() => {
      
   }),
   test("verificar se o endereço do cliente está cadastrado",() => {
      
   }),
   test("cadastrando endereço se não houver",() => {
      
   }),
   test("preenchendo cartão",() => {
      expect(2).toBe(3)
   })
})